# Инструкция по настройке Souffle

## Шаг 1: Настройка Supabase

1. Перейдите на [supabase.com](https://supabase.com) и создайте аккаунт
2. Создайте новый проект
3. Дождитесь завершения создания базы данных

## Шаг 2: Применение миграций

1. В панели Supabase перейдите в раздел **SQL Editor**
2. Скопируйте содержимое миграции из консоли (она была создана автоматически)
3. Или используйте Supabase CLI для применения миграций

Миграция создаст следующие таблицы:
- `worlds` - миры обучения
- `scenes` - сцены/упражнения
- `assets` - медиа файлы
- `culture_cards` - культурные факты
- `user_profiles` - профили пользователей
- `user_progress` - прогресс обучения
- `user_events` - аналитика

## Шаг 3: Добавление начальных данных

### Вариант A: Через SQL Editor

Выполните следующий SQL:

```sql
INSERT INTO worlds (slug, title_fr, title_ru, description, "order", color_palette)
VALUES (
  'moi-et-le-monde',
  'Moi et le monde',
  'Я и мир',
  'Знакомство с собой, эмоциями и базовыми фразами через визуальные образы',
  1,
  '{"primary": "#F5B6B4", "secondary": "#A7D8F0", "accent": "#F9E79F", "background": "#F5F3EF"}'::jsonb
);
```

### Вариант B: Через скрипт инициализации

1. Откройте файл `scripts/init-db.html` в браузере
2. Введите URL и Anon Key вашего Supabase проекта
3. Нажмите "Initialize Database"

Скрипт автоматически создаст:
- Мир "Moi et le monde"
- 10 начальных сцен обучения

## Шаг 4: Настройка переменных окружения

1. Скопируйте `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. В панели Supabase перейдите в **Settings → API**

3. Скопируйте значения в `.env`:
```
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

## Шаг 5: Установка и запуск

1. Установите зависимости:
```bash
npm install
```

2. Запустите проект в режиме разработки:
```bash
npm run dev
```

3. Откройте браузер на `http://localhost:5173`

## Шаг 6: Проверка работы

При первом запуске вы должны увидеть:
- Главный экран с картой островов
- Остров "Moi et le monde" с акварельным эффектом
- При клике на остров - список из 10 сцен
- Первая сцена "Le miroir" доступна для прохождения

## Дополнительная настройка

### Добавление своих сцен

Сцены хранятся в таблице `scenes`. Каждая сцена имеет:
- `world_id` - ID мира
- `slug` - уникальный идентификатор
- `type` - тип упражнения
- `order` - порядок в списке
- `title_fr` - название на французском
- `content_spec` - JSON с настройками сцены
- `is_core` - флаг базовой сцены

### Добавление культурных карточек

```sql
INSERT INTO culture_cards (word_or_phrase, etymology, fun_fact)
VALUES (
  'bizarre',
  'Происходит от испанского "bizarro", что означало "смелый", а не "странный"',
  'В русский язык слово пришло через французский, полностью сохранив своё современное значение'
);
```

### Настройка RLS (Row Level Security)

Политики безопасности уже настроены:
- Контент (worlds, scenes, assets, culture_cards) - доступен всем
- Пользовательские данные (progress, events) - только владельцу

## Развёртывание в продакшн

### Vercel

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Разверните проект:
```bash
vercel
```

3. Добавьте переменные окружения в настройках проекта на Vercel

### Netlify

1. Создайте `netlify.toml`:
```toml
[build]
  command = "npm run build"
  publish = "dist"
```

2. Разверните через Netlify CLI или GitHub интеграцию

3. Добавьте переменные окружения в настройках сайта

## Troubleshooting

### Ошибка подключения к Supabase

Проверьте:
- Правильность URL и ключа в `.env`
- Что файл `.env` находится в корне проекта
- Что вы перезапустили dev сервер после изменения `.env`

### Не отображаются сцены

Проверьте:
- Что миграции применены
- Что начальные данные добавлены
- Откройте консоль браузера для просмотра ошибок

### Не работает озвучка

Web Speech API работает только:
- В браузерах Chrome/Edge/Safari
- Требует подключения к интернету
- В некоторых браузерах требует HTTPS

## Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте логи Supabase в разделе Logs
3. Убедитесь, что все миграции применены

---

Приятного обучения! 🌸
